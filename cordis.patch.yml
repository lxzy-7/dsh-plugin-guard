# dsh-plugin-guard: install safety net for DeepSeek Harness.
# Single bundle row: tools + incident prompt + 备份管理 HTTP API (registered
# only when a webServer exists) + the 设置 > 插件 > 插件配置 settings card
# (rc.7 plugin-owned settings surface). No second apiOnly row is needed.
- insert:
    - id: guard
      name: 'dsh-plugin-guard'
